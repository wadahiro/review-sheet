{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "review-sheet dictionary",
  "description": "<product>@<version>.yml \u2014 what a PRODUCT says about its own settings. Written wholesale by an extraction; a hand edit belongs in <product>@<version>.overlay.yml, whose accepted fields are deliberately narrower.",
  "type": "object",
  "required": [
    "product",
    "version",
    "parameters"
  ],
  "additionalProperties": false,
  "properties": {
    "product": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "string",
      "minLength": 1
    },
    "provenance": {
      "$ref": "#/definitions/langProvenance"
    },
    "coverage": {
      "enum": [
        "full",
        "partial"
      ]
    },
    "generated_by": {
      "type": "string",
      "minLength": 1
    },
    "docs_url": {
      "$ref": "#/definitions/url"
    },
    "block_label": {
      "description": "What this format's own documentation calls a block its SYNTAX leaves unnamed \u2014 logrotate(8) says \"log file definition\" for `/var/log/x { \u2026 }`, which opens with its patterns and no keyword. A document-level fact, because it is one word for every such block in the format, not a per-entry one. Absent for a format whose blocks name themselves (httpd writes `Directory`), which is most of them. The parser never supplies this: a word for a thing the file does not name is documentation, and the sheet marks it so a reader never mistakes it for their own text.",
      "$ref": "#/definitions/langText"
    },
    "parameters": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "minLength": 1
      },
      "additionalProperties": {
        "$ref": "#/definitions/parameter"
      }
    }
  },
  "definitions": {
    "provenance": {
      "enum": [
        "official",
        "community",
        "machine",
        "extracted",
        "project"
      ]
    },
    "langProvenance": {
      "anyOf": [
        {
          "$ref": "#/definitions/provenance"
        },
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "properties": {
            "en": {
              "$ref": "#/definitions/provenance"
            },
            "ja": {
              "$ref": "#/definitions/provenance"
            }
          }
        }
      ]
    },
    "langText": {
      "anyOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "properties": {
            "en": {
              "type": "string",
              "minLength": 1
            },
            "ja": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      ]
    },
    "url": {
      "type": "string",
      "pattern": "^https?://"
    },
    "parameter": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "aliases": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1
        },
        "label": {
          "$ref": "#/definitions/langText"
        },
        "description": {
          "$ref": "#/definitions/langText"
        },
        "default": {
          "oneOf": [
            {
              "type": [
                "string",
                "number",
                "boolean"
              ]
            },
            {
              "type": "object",
              "minProperties": 2,
              "propertyNames": {
                "minLength": 1
              },
              "additionalProperties": {
                "type": [
                  "string",
                  "number",
                  "boolean"
                ]
              }
            }
          ]
        },
        "type": {
          "type": "string",
          "minLength": 1
        },
        "scope": {
          "type": "string",
          "minLength": 1
        },
        "group": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ]
        },
        "unit": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "enum": [
            "value",
            "container"
          ]
        },
        "ui": {
          "enum": [
            "editable",
            "readonly",
            "absent"
          ]
        },
        "options": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/definitions/paramOption"
          }
        },
        "secret": {
          "type": "boolean"
        },
        "docs_url": {
          "$ref": "#/definitions/url"
        },
        "provenance": {
          "$ref": "#/definitions/langProvenance"
        },
        "presence": {
          "oneOf": [
            {
              "const": true
            },
            {
              "type": "object",
              "required": [
                "label"
              ],
              "properties": {
                "label": {
                  "type": [
                    "string",
                    "object"
                  ]
                }
              },
              "additionalProperties": false
            }
          ]
        }
      },
      "allOf": [
        {
          "$comment": "A container groups other objects and has no value of its own, so it has no default either \u2014 and a default here is an object, which is not what the field may hold. This pairing is what materialize reads to keep such an entry out of the inventory.",
          "if": {
            "properties": {
              "kind": {
                "const": "container"
              }
            },
            "required": [
              "kind"
            ]
          },
          "then": {
            "not": {
              "required": [
                "default"
              ]
            }
          }
        },
        {
          "not": {
            "required": [
              "presence",
              "options"
            ]
          }
        },
        {
          "if": {
            "required": [
              "presence"
            ]
          },
          "then": {
            "properties": {
              "default": {
                "enum": [
                  "present",
                  "absent"
                ]
              }
            }
          }
        }
      ]
    },
    "paramOption": {
      "type": "object",
      "required": [
        "value"
      ],
      "additionalProperties": false,
      "properties": {
        "value": {
          "type": "string",
          "description": "The STORED form, verbatim \u2014 what a config file holds. Never a display form."
        },
        "label": {
          "$ref": "#/definitions/langText"
        }
      }
    }
  }
}
