---
# Placeholder inventory. Real hosts live in your own VCS, not in this example;
# the point here is the `web`/`db` group bindings for group_vars/{web,db}.yml
# (the production values the review sheet reads). In production you'd run this
# over SSH.
all:
  children:
    web:
      hosts:
        web-production: {}
    db:
      hosts:
        db-production: {}
