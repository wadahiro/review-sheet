# What the RPM shipped, before this project touched it — httpd's own
# defaults, committed here so the sheet can state the delta instead of
# leaving a disabled directive invisible.
ServerRoot "/etc/httpd"
Listen 80
KeepAlive Off
